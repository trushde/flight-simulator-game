using UnityEngine;
using UnityEngine.UI;

public class lift_scr : MonoBehaviour
{

    public Rigidbody ac;
    public Text lift;
  
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
            
    }


}
